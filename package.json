{
  "name": "shiori_converter",
  "version": "2.0.2",
  "description": "SHIORI Protocol version converter",
  "keywords": [
    "ikagaka",
    "ukagaka",
    "shiori"
  ],
  "main": "./dist/lib/shiori_converter.js",
  "types": "./dist/lib/shiori_converter.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepare": "npm run dist",
    "prepublishOnly": "npm run lint && npm run test",
    "dist": "npm run node",
    "node": "shx rm -rf dist && cross-env NODE_ENV=production tsc",
    "lint": "npm run eslint && npm run tslint",
    "eslint": "eslint *.js",
    "tslint": "tslint -p .",
    "test": "npm run test-node",
    "test-node": "cross-env NODE_ENV=test nyc mocha",
    "docdist": "cd doc && git clean -fd && git rm -rf * && cd .. && npm run doc && cd doc && touch .nojekyll && git add . && git commit -m doc && git push",
    "doc": "typedoc --mode modules --out doc --readme Readme.md --name \"API Document\" --disableOutputCheck --includeDeclarations --excludeNotExported lib && cd doc && git status && cd .."
  },
  "dependencies": {},
  "peerDependencies": {
    "shiorijk": "^1.0.0"
  },
  "devDependencies": {
    "@types/mocha": "^5.0.0",
    "@types/power-assert": "^1.4.29",
    "cross-env": "^5.1.3",
    "eslint": "^5.0.0",
    "eslint-config-narazaka": "^1.0.1",
    "espower-typescript": "^9.0.0",
    "mocha": "^5.0.0",
    "nyc": "^12.0.1",
    "power-assert": "^1.4.4",
    "shiorijk": "^1.0.0",
    "shx": "^0.3.0",
    "tslint": "^5.9.1",
    "typedoc": "^0.11.0",
    "typescript": "^2.7.2"
  },
  "url": "https://github.com/Narazaka/shiori_converter.js",
  "author": "narazaka",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Narazaka/shiori_converter.js.git"
  }
}
